{
    "agent": "memory",
    "description": "Semantic persistent memory store",
    "maxMemoryChars": 6000,
    "tools": [
        {
            "name": "memory_store",
            "description": "Store anything that might be useful later — observations, failed approaches, user preferences, working context, half-formed hunches. OVER-STORE rather than under-store. The Dreaming System automatically organizes, connects, and compresses memories into a structured map every hour. Imperfect, redundant, or partial memories are fine — they get consolidated during dreaming. You can always update or delete later. Only required field: a short description.",
            "inputSchema": {
                "type": "object",
                "properties": {
                    "description": { "type": "string", "description": "What did you learn? Be specific — this is all you'll see in search results later" },
                    "category": { "type": "string", "description": "Domain tag for filtering. Examples: 'notes', 'personal preference', 'hardware', 'coding style', 'architecture'. Defaults to 'notes' if omitted." },
                    "confidence": { "type": "number", "description": "How sure are you? 0-1 scale (default 0.5). 1.0 = verified fact, 0.0 = wild guess" },
                    "data": { "type": "string", "description": "Extended details (only visible via memory_get, hidden from listings). Use for context that would clutter search results." }
                },
                "required": ["description"]
            }
        },
        {
            "name": "memory_recall",
            "description": "CALL THIS FIRST at every session start. Your memories are organized into a Map (injected at session start) showing clusters, priorities, and relationships. Use recall to dive deeper into specific topics, find recent memories not yet in the map, or verify details. Semantic matching finds related memories even with different wording.",
            "inputSchema": {
                "type": "object",
                "properties": {
                    "query": { "type": "string", "description": "What are you looking for? Natural language — semantic matching finds related memories even with different wording" },
                    "limit": { "type": "number", "description": "Max results (default: 5)" },
                    "category": { "type": "string", "description": "Filter to a specific domain tag" }
                },
                "required": ["query"]
            }
        },
        {
            "name": "memory_get",
            "description": "Retrieve a single memory's full content including the data payload. Use when memory_recall returns a result with [has data] tag and you need the details.",
            "inputSchema": {
                "type": "object",
                "properties": {
                    "id": { "type": "number", "description": "Memory ID to retrieve" }
                },
                "required": ["id"]
            }
        },
        {
            "name": "memory_forget",
            "description": "Delete a memory by ID. Use when you find outdated, incorrect, or superseded memories during recall or review.",
            "inputSchema": {
                "type": "object",
                "properties": {
                    "id": { "type": "number", "description": "Memory ID to delete" }
                },
                "required": ["id"]
            }
        },
        {
            "name": "memory_list",
            "description": "Browse all stored memories or filter by category. Use for periodic review, cleanup sessions, or curiosity about what you already know.",
            "inputSchema": {
                "type": "object",
                "properties": {
                    "category": { "type": "string", "description": "Filter to a specific domain tag" }
                }
            }
        },
        {
            "name": "memory_update",
            "description": "Update an existing memory when your understanding improves. Re-embeds automatically if text changes. Prefer over delete+store to preserve ID history.",
            "inputSchema": {
                "type": "object",
                "properties": {
                    "id": { "type": "number", "description": "Memory ID to update" },
                    "description": { "type": "string", "description": "Updated short summary" },
                    "category": { "type": "string", "description": "Updated domain tag" },
                    "confidence": { "type": "number", "description": "Updated confidence 0-1" },
                    "data": { "type": "string", "description": "Updated extended content" }
                },
                "required": ["id"]
            }
        },
        {
            "name": "memory_overview",
            "description": "⚠️ CALL THIS FIRST at session start. Shows everything you know — organized into thematic clusters, cross-topic connections, and priority scores. This is your accumulated knowledge topology. Without it, you're operating blind. Use it to understand what you already know before making decisions or storing new memories.",
            "inputSchema": {
                "type": "object",
                "properties": {
                    "format": { "type": "string", "description": "Output format: 'summary' (clusters, bridges, top nodes) or 'full' (all nodes with details). Default: 'summary'.", "enum": ["summary", "full"] }
                }
            }
        }
    ]
}
